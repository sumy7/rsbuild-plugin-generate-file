{
  "name": "example",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rsbuild dev --open",
    "build": "rsbuild build",
    "preview": "rsbuild preview"
  },
  "dependencies": {
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@rsbuild/core": "^0.7.10",
    "@rsbuild/plugin-vue": "^0.7.10",
    "typescript": "^5.3.0",
    "rsbuild-plugin-generate-file": "workspace:*"
  }
}
